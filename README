This is a program that does a simple BFS maze solve with shortest path finding.

The end goal is to have a maze solver that can solve a maze that someone might
draw on paper (including overlapping lines).
